# Placeholder manifest — CHANGE_ME (base64 of the literal string "CHANGE_ME") must be
# replaced before deploy. Generate real keys with `npx web-push generate-vapid-keys` and
# inject via Vault CSI (infrastructure/k8s/vault) or `kubectl create secret` at apply time —
# never commit real VAPID keys to this file.
apiVersion: v1
kind: Secret
metadata:
  name: notification-secret
  namespace: ebanking-backend
type: Opaque
data:
  vapid-public-key: Q0hBTkdFX01F
  vapid-private-key: Q0hBTkdFX01F
