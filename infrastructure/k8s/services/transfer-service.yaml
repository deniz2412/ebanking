apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfer-service
  namespace: ebanking-backend
  labels:
    app: transfer-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transfer-service
  template:
    metadata:
      labels:
        app: transfer-service
        tier: backend
    spec:
      containers:
      - name: transfer-service
        image: ebanking/transfer-service:dev
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Development"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: mssql-secret
              key: transfer-connection-string
        - name: ConnectionStrings__Kafka
          value: "redpanda.svc.cluster.local:9092"
        - name: Services__AccountService
          value: "http://account-service.ebanking-backend.svc.cluster.local"
        - name: Auth__Authority
          value: "https://ebank.local/auth/realms/ebanking"
        - name: Auth__Audience
          value: "transfer-service"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: app-tmp
          mountPath: /app/tmp
      volumes:
      - name: tmp
        emptyDir: {}
      - name: app-tmp
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: transfer-service
  namespace: ebanking-backend
  labels:
    app: transfer-service
spec:
  selector:
    app: transfer-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
